// Teste de autocomplete
import { GameConfig } from '@/types'
import {as} from '@/types'
// Digite aqui e veja se sugere imports:
// import { } from '@/'

const test: GameConfig = {
  gameType: 'flags',
  location: 'brazil', 
  difficulty: 'easy'
}
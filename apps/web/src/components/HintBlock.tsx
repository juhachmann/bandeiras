
export default function HintBlock ({ }) {

    // Deve receber: lado da tela, se deve ficar estático na tela ou acompanhar o scroll 
    // Deve recber o texto em dois pedaços: o resumo básico e o saiba mais

    return (
        <div>Bloco de dicas</div>
    )

}